var app = angular.module("legendaryApp",[]);

app.controller("legendaryController", function ($scope, $http){

	$scope.loadcomment() = function ()	{
							//$http.get("http://127.0.0.1:8080/db.js").success(function (data){ $scope.comment=data; alert(data);

//}).error(function (){ alert("unexpected error");

$scope.comment="ello , kopito, paneer tikka masala";
alert("ello ... banana");
}}			      );							
				

